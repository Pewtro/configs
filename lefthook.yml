pre-commit:
  parallel: true
  jobs:
    - run: pnpm --silent lint:fix {staged_files}
      glob: '*.{ts,tsx,js,jsx,mjs}'

    - run: pnpm oxfmt --no-error-on-unmatched-pattern {staged_files}
      glob: '*.{ts,tsx,js,jsx,mjs,md,mdx,yaml,yml,json}'
